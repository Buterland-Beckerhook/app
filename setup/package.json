{
	"name": "setup",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"setup:schema": "tsx src/setup-schema.ts",
		"setup:permissions": "tsx src/setup-permissions.ts",
		"setup:all": "npm run setup:schema && npm run setup:permissions"
	},
	"dependencies": {
		"@directus/sdk": "^21.1.0"
	},
	"devDependencies": {
		"@types/node": "^22.15.0",
		"tsx": "^4.19.0",
		"typescript": "^5.8.0"
	}
}
